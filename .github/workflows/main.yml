name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      with:
        repository: xtaci/kcptun
        ref: v20191105
    - name: show current folder
      run: ls
    - name: show
      run: pwd

